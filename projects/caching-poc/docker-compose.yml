version: "3.8"

services:
  app:
    build: .
    ports:
      - "8085:8080"
    environment:
      - REDIS_ADDR=redis:6379
      - CACHE_DISABLED=false
      - DB_LATENCY_MS=50
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
